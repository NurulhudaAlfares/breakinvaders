from setuptools import setup, find_packages

setup(
    name="breakinvaders",
    version="1.0.0",
    packages=find_packages(),
    install_requires=[
        "pygame>=2.6.1",
    ],
    python_requires=">=3.8",
) 